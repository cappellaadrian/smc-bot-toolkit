# Copy this file to .streamlit/secrets.toml (which is gitignored) for local
# development, or paste the body into Streamlit Community Cloud's
# App settings -> Secrets when deploying.

ANTHROPIC_API_KEY = "sk-ant-..."

# Required only for the Live Performance page (the bot's runtime metrics).
# Leave blank until you've created a Supabase project and applied the
# supabase/migrations/20260506_init.sql migration.
SUPABASE_URL = ""
SUPABASE_KEY = ""
